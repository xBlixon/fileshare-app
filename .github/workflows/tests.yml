name: tests

on: push

permissions:
  contents: read

env:
  node-version: '24'
  node-cache: 'npm'

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php-version: ['8.4', '8.5']
    env:
      APP_ENV: testing
      APP_URL: "http://127.0.0.1:8000"
      DB_DATABASE: database/dusk.sqlite
      MAIL_MAILER: log

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: PHP Setup
        uses: "./.github/actions/php-setup"
        with:
          php-version: ${{ matrix.php-version }}
          coverage: xdebug

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.node-version }}
          cache: ${{ env.node-cache }}

      - name: Install Node Dependencies
        run: npm i

      - name: Install Dependencies
        run: composer install -q --no-interaction --no-progress --prefer-dist --optimize-autoloader

      - name: Copy Environment File
        run: cp .env.example .env

      - name: Set APP_ENV to ${{ env.APP_ENV }}
        uses: './.github/actions/set-env-var'
        with:
          file: .env
          key: APP_ENV
          value: ${{ env.APP_ENV }}

      - name: Setup Dusk .env Configuration
        uses: './.github/actions/dusk-env'
        with:
          APP_ENV: ${{ env.APP_ENV }} # https://laravel.com/docs/13.x/dusk#environment-handling
          APP_URL: ${{ env.APP_URL }}
          DB_DATABASE: ${{ env.DB_DATABASE }}
          MAIL_MAILER: ${{ env.MAIL_MAILER }}

      - name: Generate Application Key
        run: php artisan key:generate

      - name: Build Assets
        run: npm run build

      - name: Start Chrome Driver
        run: ./vendor/laravel/dusk/bin/chromedriver-linux --port=9515 &

      - name: Run Laravel Server
        run: php artisan serve --no-reload --env=dusk.${{ env.APP_ENV }} &

      - name: Run Dusk Tests
        continue-on-error: true
        run: php artisan dusk

      - name: Dump logs
        uses: './.github/actions/show-logs'
